import React from 'react'
export default function ViewToggle({ value, onChange, options=[] }) { return <div className="inline-flex items-center rounded-xl bg-slate-100 p-1">{options.map(option=>{const Icon=option.icon;const active=value===option.value;return <button key={option.value} type="button" onClick={()=>onChange(option.value)} className={['inline-flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-semibold transition-all duration-200',active?'bg-white text-slate-900 shadow-sm':'text-slate-500 hover:text-slate-800'].join(' ')}>{Icon&&<Icon className="h-4 w-4"/>}{option.label}</button>})}</div> }
